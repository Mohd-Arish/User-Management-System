<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full h-full bg-zinc-900 p-10 text-white">
        <div class="mb-8">
        <a class="text-green-400" href="/read">Read users</a>
     </div>

        <h1 class="text-3xl tracking-tight ml-2 mb-3">Update User</h1>
        <form action="/update/<%= user._id %>" method="post">
            <input class="px-3 border-2 border-zinc-800 outline-none rounded-lg bg-transparent py-2 " type="text" name="name" placeholder="name" value="<%= user.name %>">
            <input class="px-3 border-2 border-zinc-800 outline-none rounded-lg bg-transparent py-2 " type="text" name="email" placeholder="email" value="<%= user.email %>">
            <input class="px-3 border-2 border-zinc-800 outline-none rounded-lg bg-transparent py-2 " type="text" name="image" placeholder="image url" value="<%= user.image %>">
            <input class="px-3 bg-red-700 outline-none rounded-lg py-2 ml-2 mt-2 " type="submit" value="Update user">
        </form>
    </div>

    <script src=""></script>
</body>
</html>