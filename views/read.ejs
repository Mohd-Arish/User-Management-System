<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Read</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<style>
    * {
            font-family: Arial, Helvetica, sans-serif;
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        html, body {
            width: 100%;
            height: 100%;
        }

    .con {
    width: 100%;
    height: 100%;
}
</style>
<body class="bg-zinc-900 text-white min-h-screen flex flex-col">
    <div class="con w-full h-full bg-zinc-900 p-10 text-white">
        <a class="text-green-400" href="/">- Back to home</a>
        <h1 class="text-3xl tracking-tight mt-6 mb-2 ml-2">All users</h1>
        <div class="users flex flex-wrap gap-4">
           <% if( users.length > 0){ %>
            <% users.forEach((user)=>{ %>
                <div class="user w-70 p-4 bg-zinc-800 rounded-lg">
                    <div class="w-full h-64 bg-zinc-700 rounded-lg overflow-hidden">
                        <img class="w-full h-full object-cover object-top" src="<%= user.image %>" alt="">
                    </div>
                    <h3 class="text-xl mt-3"><%= user.name %></h3>
                    <h5 class="text-zinc-400 mb-5"><%= user.email %></h5>
                    <div class="flex gap-20 mt-2">
                        <a class="text-white" href="/edit/<%= user._id %>">Edit user</a>
                        <a class="text-green-400" href="/delete/<%= user._id %>">Delete user</a>
                    </div>
                </div>
        <% }) %> 
         <%   } else { %>
            <h1 class="text-xl tracking-tight text-zinc-600 ml-2">No users till now</h1>
       <%  } %>
        </div>
    </div>
    
</body>
</html>
